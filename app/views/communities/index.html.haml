- client = DeviceDetector.new(request.user_agent)
- type = client.device_type == 'smartphone' ? 'smartphone' : 'base'

%p#notice= notice
%h1.text-center= link_to t('base.communities'), communities_path, alt: 'index communities', class: 'link-dark'

.d-flex.justify-content-center
  %ul#tabCommunities.nav.nav-tabs{:role => "tablist"}
    %li.nav-item
      %a#all-tab.nav-link.active{"aria-controls" => "all", "aria-selected" => "true", "data-toggle" => "tab", :href => "#all", :role => "tab"} Todas
    %li.nav-item
      %a#category-tab.nav-link{"aria-controls" => "category", "aria-selected" => "false", "data-toggle" => "tab", :href => "#category", :role => "tab"} Categorías
    %li.nav-item
      %a#comunas-tab.nav-link{"aria-controls" => "comunas", "aria-selected" => "false", "data-toggle" => "tab", :href => "#comunas", :role => "tab"} Comunas

= render "communities/#{type}/index"

-# %tbody
  - @communities.each do |community|
    %tr
      %td= community.name
      %td= community.about
      %td= community.address
      %td= community.phone
      %td= community.email
      %td= link_to 'Show', community
      %td= link_to 'Edit', edit_community_path(community)
      %td= link_to 'Destroy', community, method: :delete, data: { confirm: 'Are you sure?' }
      -# = link_to 'New Community', new_community_path
