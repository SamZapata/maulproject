.show-community
  %nav{"aria-label" => "breadcrumb"}
    .bg-bar-community
      .container
        %ol.breadcrumb
          %li.breadcrumb-item
            %a.link-white{:href => "/"}
              %i.fas.fa-home
          %li.breadcrumb-item
            %a.link-white{:href => communities_path}= t('base.communities')
          %li.breadcrumb-item.active{"aria-current" => "page"}= @community.name
  .container.text-center
    %p#notice= notice
    - if user_signed_in? && current_user.id == @community.user_id
      .show-community-edit
        = link_to edit_community_path(@community), class: 'link-dark' do
          %i.fa.fa-edit
    %h1.mt-3= @community.name
    %hr.mb-2
    .community-networks-section
      = networks_section @community
    .mt-3= image_tag(@community.picture, width: 200) if @community.picture.attached?
    .mt-3.card
      .card-body.text-left
        %strong= t('community.about_community')
        %p
          = @community.about

        - if @community.maul_plans.count > 0
          %em ¿Dónde nos reunimos?
          %p
            - @community.maul_plans.each do |community_site|
              = link_to community_site.site.name, community_site.site
              %br
        - elsif @community.address.present?
          %em ¿Dónde nos reunimos?
          %p= @community.address

        - if @community.phone.present?
          %em Teléfono(s) de contacto:
          %p= @community.phone

        - if @community.email.present?
          %em correo electrónico:
          %p= @community.email

    .mt-3.card
      .card-body
        %strong Eventos que realizamos


-# ========== mobile version ==========
-# %nav{"aria-label" => "breadcrumb"}
  .breadcrumb.bg-primary
    .container
      %ol.breadcrumb.bg-primary
        -# %li.breadcrumb-item
          %a.link-dark{:href => "/"}
            %i.fas.fa-home
        %li.breadcrumb-item
          %a.link-dark{:href => communities_path}= t('base.communities')
        -# %li.breadcrumb-item.active{"aria-current" => "page"}= @community.name
-# .show-community
  .bg-bar-community
    .container
      %h2.text-left.link-white= @community.name

  .container
    %p#notice= notice
    / community image
    = image_tag 'https://42f2671d685f51e10fc6-b9fcecea3e50b3b59bdc28dead054ebc.ssl.cf5.rackcdn.com/illustrations/good_team_m7uu.svg', width: '50%'

    - missingData = []
    %p
      = @community.about
    = verifyFields @community
    - if @community.address.present?
      %p
        %strong Dirección:
        = @community.address
    - else
      - missingData << @community.address

    - if @community.phone.present?
      %p
        %strong Teléfono de contacto:
        = @community.phone
    - else
      - missingData << @community.phone

    - if @community.email.present?
      %p
        %strong Correo electrónico:
        = @community.email
    - else
      - missingData << @community.email

    = link_to 'Edit', edit_community_path(@community)
    |
    = link_to 'Back', communities_path
